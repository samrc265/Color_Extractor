# Color Extractor: Image Palette Generator 🎨

**Color Extractor** is a web-based tool that analyzes images to identify and extract their dominant colors. Simply upload an image, and the application will generate a color palette, providing the hex codes for design and creative projects.

## ✨ Key Features
* **Image Analysis:** Scans uploaded images to determine the most prominent colors.
* **Palette Generation:** Displays a clean list of extracted colors with their corresponding HEX values.
* **Simple Web Interface:** A user-friendly upload system built with HTML templates.
* **Modular Logic:** Separation of backend logic (`color_extractor.py`) and web routing (`app.py`).

## 🛠 Tech Stack
* **Language:** Python
* **Framework:** Flask
* **Frontend:** HTML / CSS
* **Libraries:** Pillow (PIL) / Colorgram

## 🚀 How to Build
1. Clone the repository:
   `git clone https://github.com/samrc265/Color_Extractor.git`
2. Open the project in your IDE (VS Code / PyCharm).
3. Install the required dependencies:
   `pip install flask colorgram.py`
4. Run the application:
   `python app.py`
5. Open your web browser and go to the local URL (usually `http://127.0.0.1:5000`).

---
